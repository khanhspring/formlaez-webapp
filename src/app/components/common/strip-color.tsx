import { FC } from 'react';

type Props = {
}

const StripColor: FC<Props> = ({ }) => {
    return (
        <div className="h-1 w-full flex-shrink-0 bg-strip-color" />
    );
}

export default StripColor;
